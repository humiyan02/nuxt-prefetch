import {
  src_default
} from "./chunk-2H76JNCY.mjs";
import "./chunk-LYFLWIEG.mjs";

// src/nuxt.ts
import defu from "defu";
function nuxt_default(inlineOptions = {}) {
  const options = defu(inlineOptions, this.nuxt.options.scriptSetup);
  this.extendBuild((config) => {
    config.plugins = config.plugins || [];
    config.plugins.unshift(src_default.webpack(options));
  });
  this.nuxt.hook("vite:extend", async (vite) => {
    vite.config.plugins = vite.config.plugins || [];
    vite.config.plugins.push(src_default.vite(options));
  });
}
export {
  nuxt_default as default
};
